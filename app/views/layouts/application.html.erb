<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %> / Twitter</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="container-fluid">


    <div class="row">
    <p id="notice"><%= notice %></p>
    <p id="alert"><%= alert %></p>

      <div class="col-2 border-right px-4 vh-100 position-fixed">
        <div class="d-flex flex-column">
          <%= link_to fa_icon("twitter"), tweets_path, class: "text-primary fa-4x" %>
          <div class="mt-2 d-flex flex-column">
            
            <%= link_to(tweets_path) do %>
              <div class="d-flex align-items-center mt-4 text-body">
                <div class="w-25"><%= fa_icon("home", class: "fa-3x") %></div>
                <h4 class="ml-4 w-50 font-weight-bold d-none d-md-block">Home</h4>
              </div>
            <% end %>
            
            <% if user_signed_in? %>

              <%= link_to(destroy_user_session_path, method: "delete") do %>
                <div class="d-flex align-items-center mt-4 text-body">
                  <div class="w-25"><%= fa_icon("sign-out", class: "fa-3x") %></div>
                  <h4 class="ml-4 w-50 font-weight-bold d-none d-md-block">Log out</h4>
                </div>
              <% end %>

            <% else %>

              <% unless current_page?(new_user_session_path) %>
                <%= link_to(new_user_session_path) do %>
                  <div class="d-flex align-items-center mt-4 text-body">
                    <div class="w-25"><%= fa_icon("sign-in", class: "fa-3x") %></div>
                    <h4 class="ml-4 w-50 font-weight-bold d-none d-md-block">Log in</h4>
                  </div>
                <% end %>
              <% end %>

              <% unless current_page?(new_user_registration_path) %>
                <%= link_to(new_user_registration_path) do %>
                  <div class="d-flex align-items-center mt-4 text-body">
                    <div class="w-25"><%= fa_icon("user-plus", class: "fa-3x") %></div>
                    <h4 class="ml-4 w-50 font-weight-bold d-none d-md-block">Sign up</h4>
                  </div>
                <% end %>
              <% end %>
            
            <% end %>
          </div>
        </div>
      </div>

      <div class="col-10 offset-2 vh-100 p-0">
        <%= yield %>
      </div>

    </div>
  </body>
</html>
