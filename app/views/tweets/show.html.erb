<%= render 'tweet', resource: @tweet %>
<div>
  <% unless @tweet.likes.length == 0 %>
    <div style="display: flex;">
      <% @tweet.likes.each do |like| %>
        <figure class="text-center mx-3 my-3">
          <img src="<%= like.user_pic_url %>" style="width: 50px; height: 50px;" class="rounded-circle" alt="profile pic">
          <figcaption><%= like.user %> liked this tweet</figcaption>
        </figure>
      <% end %>
    </div>
  <% else %>
    <p>This tweet hasn't been liked yet.</p>
  <% end %>
</div>